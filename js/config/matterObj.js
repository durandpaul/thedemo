"use strict";

// use it globally

// Call Methods from Matter JS library
const Engine = Matter.Engine,
  World = Matter.World,
  Bodies = Matter.Bodies,
  Body = Matter.Body;

// init var to create engine
var engine;
